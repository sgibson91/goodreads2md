---
author:
  - "{{ author }}"
date-last-read: "{{ date_last_read }}"
date-last-started: "{{ date_last_started }}"
genre: "{{ genre }}"
goodreads: "{{ url }}"
owned: "{{ owned }}"
rating: "{{ rating }}"
re-read: "{{ re_read }}"
series: "{{ series }}"
status: "{{ status }}"
---

![]({{ cover_url }})

## Summary

{{ description }}
